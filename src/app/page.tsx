
import Header from "@/components/Header";
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Portal-POC',
  description: 'Welcome to Portal-POC',
};

export default function Home() {
  return (
    <div></div>
  );
}
